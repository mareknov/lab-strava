# Iteration 03

This 

- generate class hierarchy, interfaces, DTOs, JPAs to represent a `Activity` object
- generate also API layer to create and retrieve `Activity` object
- generate JPA to persist `Activity` object in Postgres database
- generate unit and controller tests for `Activity` object

## Requirements

- suggest fields that represent Strava activity
- ask questions to specify what to include and what not to include from Strava API documentation
